[package]
name = "fhe-memory-lab"
version = "0.0.1"
edition = "2024"

[dependencies]
tfhe = { version = "*", features = ["boolean", "shortint", "integer"] }

[profile.release]
opt-level = 3
lto = "thin"
